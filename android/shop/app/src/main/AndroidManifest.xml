<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.vcvb.chenyu.shop">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission
        android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"
        tools:ignore="ProtectedPermissions" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".ShopApplication"
        android:allowBackup="true"
        android:icon="@drawable/shop_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"

        android:theme="@style/AppThemeMain">
        <activity android:name=".LaunchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoadResActivity"
            android:alwaysRetainTaskState="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:process=":mini"
            android:screenOrientation="portrait" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/AppThemeMain" />

        <activity
            android:name=".goods.GoodsDetailActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".login.RegisterActivity"
            android:label="@string/login"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".mycenter.OrderActivity"
            android:label="@string/order_list"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".mycenter.MyCollectionActivity"
            android:label="@string/collection"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".mycenter.AddressActivity"
            android:label="@string/address"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".mycenter.ModifyAddressActivity"
            android:label="@string/add_address"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".mycenter.BrowseActivity"
            android:label="@string/browse"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".mycenter.userinfo.UserInfoActivity"
            android:label="@string/personal_center"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".mycenter.userinfo.UserLogoActivity"
            android:label="@string/all_photo"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".mycenter.userinfo.UserNickActivity"
            android:label="@string/nick_title"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".mycenter.userinfo.UserAccountActivity"
            android:label="@string/account_title"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".mycenter.userinfo.UserRealNameActivity"
            android:label="@string/real_title"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".search.SearchInfoActivity"
            android:label="@string/search"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".search.SearchActivity"
            android:label="@string/search"
            android:theme="@style/AppTheme" />

        <!--<receiver android:name=".receiver.Receiver">-->
        <!--<intent-filter>-->
        <!--<action android:name="LoginClick" />-->
        <!--</intent-filter>-->
        <!--</receiver>-->
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.vcvb.chenyu.shop.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
    </application>

</manifest>